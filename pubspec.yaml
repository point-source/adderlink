name: adderlink
description: A dart library for controlling Adderlink devices on an IP network
version: 4.0.0-dev.1
homepage: https://github.com/point-source/adderlink

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  chopper: ^6.0.0
  dart_mappable: ^3.1.1
  xml2json: ^6.1.0

dev_dependencies:
  build_runner: ^2.3.2
  chopper_generator: ^6.0.0
  dart_code_metrics: ^5.1.0
  dart_mappable_builder: ^3.1.1
  lints: ^2.0.0
  test: ^1.16.0
